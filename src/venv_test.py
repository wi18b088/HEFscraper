# Check if Python interpreter is set to the right path after creating venv
import sys
print(sys.path)

# install requirements from requirements.txt (python -m pip install -r requirements.txt)
# Don't forget to install pylint for vscode/codium 